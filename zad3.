#include <stdio.h>
#include <math.h>
 
int main(void){
    double a,b,c;
    
    scanf ("%lf %lf %lf", &a, &b, &c);
    
        double p=(a+b+c)/2;
        double S=1;
        S=sqrt(p*(p-a)*(p-b)*(p-c));
        printf ("%lf\n", S);
    return 0;
    }
